interface:
  display_name: "Jira REST"
  short_description: "Use Jira via REST direta no Codex"
  default_prompt: "Use $jira-rest para consultar ou operar no Jira via API REST direta, mantendo decisoes no agente principal e delegando apenas buscas e operacoes mecanicas quando os parametros ja estiverem definidos."
