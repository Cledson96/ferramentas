interface:
  display_name: "Jira REST"
  short_description: "Use Jira via REST direta no Codex"
  default_prompt: "Use $jira-rest para consultar ou operar no Jira via API REST direta, com comentarios estruturados, transicoes e worklog em portugues."
